{
  "version": 2,
  "buildCommand": "npm run build -w packages/shared-types && npm run build -w apps/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
