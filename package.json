{
  "name": "worksuite",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web":  "npm -w apps/web run dev",
    "dev:api":  "npm -w apps/api run dev",
    "build:web": "npm -w apps/web run build",
    "build:api": "npm -w apps/api run build",
    "build:packages": "npm -w packages/shared-types run build && npm -w packages/i18n run build && npm -w packages/ui run build && npm -w packages/jira-client run build",
    "build": "npm run build:packages && npm run build:web && npm run build:api",
    "typecheck": "tsc --build --verbose",
    "lint": "eslint packages apps --ext .ts,.tsx"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
